<div class="heading">
  <div class="heading-text">メモアプリ</div>
  <a class="btn heading-btn" href="/memos/new">追加</a>
</div>
<ul>
  <% @memos.each do |memo| %>
    <li><a href="/memos/<%= "#{memo[:id]}" %>"><%== memo[:title] %></a></li>
  <% end %>
</ul>
